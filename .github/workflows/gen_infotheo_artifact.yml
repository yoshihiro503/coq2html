on:
  push:
    branches: ["mca2html"]
  pull_request:
  schedule:
    - cron: 0 0 * * * # every morning

jobs:
  generate-artifact:
    runs-on: ubuntu-latest
    continue-on-error: true  # ← If a job fails, do not make it a total failure
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: set the variables for push event
        if: ${{ github.event_name == 'push' }}
        run: |
          branch_name=${{ github.ref_name }}
          short_sha=$(git rev-parse --short ${{ github.sha }})
          echo "destination_dir=sample_"$branch_name_$short_sha >> $GITHUB_ENV

      - name: set the variables for PR event
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          branch_name=${{ github.head_ref }}
          echo "destination_dir=sample_$branch_name" >> $GITHUB_ENV

      - name: set the variables for scheduled event
        if: ${{ github.event_name == 'schedule' }}
        run: |
          name=${{ github.action }}
          echo "destination_dir=nightly_$name" >> $GITHUB_ENV

      - name: Clone Infotheowith a specific tag
        uses: actions/checkout@v4
        with:
          repository: affeldt-aist/infotheo
          path: infotheo
          fetch-tags: 0.9.4

      - name: Setup Graphviz
        uses: ts-graphviz/setup-graphviz@v2

      - name: Set-up OCaml
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: 4.14
          opam-repositories: |
            coq-released: https://coq.inria.fr/opam/released
            default: https://github.com/ocaml/opam-repository.git

      - name: Build rocqnavi
        run: opam install -y --deps-only . && opam exec -- make

      - name: Set-up Rocq
        run: |
          opam install -y coq.8.20.1

      - name: Build Infotheo
        run: cd infotheo/ && opam install -y --deps-only . && opam exec -- make all install

      - run: |
             opam exec -- tools/generate-infotheo.sh "0.9.4(rocqnavi-${GITHUB_SHA::8})"
             tree html/

      - name: Upload htmldoc as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.destination_dir }}
          path: |
            artifact
